import ComingSoon from '../components/ComingSoon';

export default function PaymentPage() {
  return (
    <ComingSoon
      service="Payment Service"
      icon="💳"
      description="Our secure payment processing service is currently being deployed. It will support Stripe, PayPal and other providers. Book now — payment will be enabled very soon!"
    />
  );
}
