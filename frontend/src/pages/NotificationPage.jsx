import ComingSoon from '../components/ComingSoon';

export default function NotificationPage() {
  return (
    <ComingSoon
      service="Notification Service"
      icon="🔔"
      description="Our real-time notification service is being deployed. You'll receive instant alerts for booking confirmations, event updates, and more."
    />
  );
}
